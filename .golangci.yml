linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign

linters-settings:
  govet:
    # Enable all checks except those that are too strict
    enable-all: true
    disable:
      - shadow
      - unusedwrite

issues:
  exclude-rules:
    # Exclude some linters from running on test files
    - path: _test\.go
      linters:
        - errcheck

run:
  timeout: 5m
